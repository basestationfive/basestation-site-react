apiVersion: v1
kind: Service
metadata:
    name: jeremyhaveardbasestationsitereact
spec:
    type: LoadBalancer
    ports:
    - port: 3000 
    selector:
        app: jeremyhaveardbasestationsitereact